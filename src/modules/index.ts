/**
 * Feature modules for the library
 * @category Modules
 */

// Modules are organized by feature:
// - gateway: Discord gateway connection
// - cache: LRU cache system
// - database: Prisma integration
// - validation: Input validation

// Note: Individual modules should be imported directly to avoid naming conflicts
// Example: import { CacheManager } from './modules/cache'

